# 実装依頼: BetterModelフォークへの物理レイヤー＆装備ソケットシステム追加

## 背景・目的

このリポジトリは BetterModel（MinecraftのDisplay Entity/パケットベースのカスタムモデルプラグイン）のフォークである。
以下の2つの機能を追加したい：

1. **装備ソケットシステム**: ベースモデル（素体）に対し、BlockBench上で配置した不可視アンカーキューブを装着点として、装備モデル（部分木）を動的にマージ・着脱できるようにする
2. **物理レイヤー（プロシージャルアニメーション）**: キーフレームアニメーションの評価後に後処理として合成される物理シミュレーション層を追加し、バネ揺れ（jiggle）・着地スカッシュ＆ストレッチ・外部インパルス（Sulfur Cube衝突など）をキーフレームなしで自動生成する

ターゲット環境: Paper 26.x（Minecraft年号バージョニング。Mojang非難読化済み、内部クラスは実名で参照可能）。Java 25。Folia対応は必須ではないが、エンティティ単位のスケジューリングを意識した設計にすること。

## 重要な制約

- **当たり判定（ヒットボックス）は一切変更しない**。物理によるボーン変形は純粋に描画演出であり、素体の判定は固定のまま
- 既存のキーフレームアニメーション機能を破壊しない。物理は「後処理の加算レイヤー」として合成する
- 合成順序は以下とする:
  ```
  最終変形 = キーフレーム評価
           × スカッシュ（着地の体積保存変形）   ← スケール乗算
           + バネ揺れ（jiggle系ボーン）        ← オフセット加算
           + インパルス（衝突・被弾）           ← 速度への直接注入
  ```

## 作業手順

### Phase 0: コードベース調査（必ず最初に行うこと）

実装に着手する前に、フォークのコード構造を調査し、以下を特定して報告すること：

- ボーンツリーの構築処理（BlockBenchモデルの読み込み〜ボーン階層の生成）
- キーフレームアニメーションの評価パイプライン（どのクラスで毎tick変形が計算されるか）
- パケット送信部分（Display Entityのtransformationをクライアントに送る箇所、補間durationの扱い）
- 空グループ/不可視キューブの扱い（描画対象ボーンのフィルタリングが既にあるか）
- 設定ファイル（モデルYAML等）の読み込み機構

調査結果をもとに、以下のPhase 1〜4の差し込み位置を具体的に設計してから実装に入ること。

### Phase 1: 装備ソケットシステム（土台）

- **アンカーボーン規約**: ボーン名が `anchor_` プレフィックスのボーンは描画をスキップする（不可視キューブの代替として明示的に非描画化）。BlockBench側ではモデラーが不可視キューブ＋ピボットを装着位置に配置する運用
- **装備部分木のマージ機構**: 装備モデルを独立したモデル定義（部分木）として管理し、実行時にベースモデルの指定アンカーボーンの子として動的にボーンツリーへマージできるようにする。親ボーンのキーフレームアニメーション・物理変形が装備へ自動伝播すること
- **着脱API**: アンカー単位で装備の装着・取り外し・付け替えができるAPIを提供する（`equip(anchorName, equipmentModel)` / `unequip(anchorName)` 的なもの）
- **Zファイティング対策**: 装備モデル側はBlockBenchのinflateで外側に膨らませる運用とするが、プラグイン側でも装着時に微小オフセットを適用できる設定項目を用意する

### Phase 2: 物理レイヤー最小実装

- **PhysicsBone コンポーネント**: ボーン名の命名規則（`jiggle_` プレフィックス）またはモデル設定YAMLで指定されたボーンに物理シミュレーションを適用する。パラメータ:
  - `stiffness` (k): バネ定数。大=硬い、小=ぷるんぷるん
  - `damping` (c): 減衰係数。小=長く揺れる
  - `inertia`: 親エンティティの加速度の伝わりやすさ
  - `gravity`: 重力影響度（垂れ下がり具合）
  - `chain`: trueの場合、親ボーンの変位が子ボーンへ伝播する（尻尾・髪・マフラーのなびき用）
- **外力の取得**: 毎tick、対象エンティティの速度差分から加速度を算出し、慣性として逆向きにボーンへ入力する
- **積分**: バネ・ダンパーモデル `F = -k×offset - c×velocity + inertia×(-加速度) + gravity項` を毎tick積分し、offset/velocityを更新。発散防止のクランプを入れること
- **着地スカッシュ＆ストレッチ**: 着地衝撃（落下速度の急減）を検出し、ルートボーンに体積保存変形を適用する:
  ```
  scaleY  = 1 - s
  scaleXZ = 1 / √(1 - s)   ← 体積保存
  ```
  sは衝撃強度に比例し、バネで自然復帰させる

### Phase 3: パケット流量の最適化

- 変位が閾値以下のボーンはパケット送信をスキップ（静止時ゼロコスト）
- 補間durationを2〜3tickに設定し、送信頻度を落としてもクライアント側で滑らかに見えるようにする
- 物理ボーン数が多い場合の負荷計測方法（timings等での確認手順）をREADME等に記載

### Phase 4: Sulfur Cube衝突インパルス

- Minecraft 26.2の Sulfur Cube（`AbstractCubeMob` 系）がモデル対象エンティティに衝突した際、衝撃ベクトル（キューブの速度×質量係数）を対象の物理レイヤー全ボーンにimpulseとして注入する
- 衝突検出はイベントまたは速度変化の監視で実装。衝撃の「方向」が揺れに反映されること（当たった方向にボーンが倒れてバネで復帰する見た目）
- 他の被弾（ノックバック等）も同じimpulse経路で流し込めるよう、インパルス注入は汎用APIとして切り出すこと

## 設定ファイルの仕様案

モデルYAMLに以下のような物理設定セクションを設けること:

```yaml
physics:
  胸:
    stiffness: 0.08
    damping: 0.12
    inertia: 1.0
    gravity: 0.3
  尻尾:
    stiffness: 0.15
    damping: 0.25
    chain: true
```

## 完了条件

- [ ] Phase 0の調査報告（差し込み位置の設計を含む）
- [ ] `anchor_` ボーンが描画されず、装備部分木がマージ・着脱できる
- [ ] 装備がベースモデルのアニメーションと物理に追従する
- [ ] `jiggle_` 指定ボーンが歩行・ジャンプ・着地に連動して物理的に揺れる（キーフレームなし）
- [ ] 着地時に体積保存のスカッシュ＆ストレッチが発生する
- [ ] Sulfur Cube衝突で衝撃方向に応じた揺れが発生する
- [ ] 静止時にパケット送信が発生しない（閾値スキップ動作）
- [ ] ヒットボックスが素体のまま固定であることの確認
- [ ] 既存のキーフレームアニメーション機能にリグレッションがないこと

## 補足

- 実装は段階的にコミットを分けること（Phase 1 → 2 → 3 → 4 の順で動作確認しながら進める）
- 不明点があれば実装前に質問すること。設計判断で迷った場合は「既存機能を壊さない」「演出と判定を分離する」の2原則を優先すること
